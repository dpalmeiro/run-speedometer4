{
  "name": "run-speedometer",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "run-speedometer": "./dist/cli.js"
  },
  "scripts": {
    "setup": "git submodule update --init && npm install && npm run build && npm link",
    "build": "tsc && chmod +x dist/cli.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
